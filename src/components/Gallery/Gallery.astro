---
// Gallery component from index.astro

// Utils
import { Picture } from "astro:assets";

// Images
import port1 from "@assets/images/projects/port1.jpg";
import port2 from "@assets/images/projects/port2.jpg";
import port3 from "@assets/images/projects/port3.jpg";
import port4 from "@assets/images/projects/port4.jpg";
import port5 from "@assets/images/projects/port5.jpg";
import port6 from "@assets/images/projects/port6.jpg";
import port7 from "@assets/images/projects/port7.jpg";
import port8 from "@assets/images/projects/port8.jpg";
import port9 from "@assets/images/projects/port9.jpg";

interface Props {
  content?: {
    eyebrow?: string;
    title?: string;
    buttonText?: string;
    buttonHref?: string;
    imageAlts?: string[];
  };
}

const { content = {} } = Astro.props as Props;
const imageAlts = content.imageAlts ?? [
  "new hallway",
  "new home construction",
  "building a new wall",
  "new kitchen",
  "apartment addition",
  "new kitchen cabinets",
  "new kitchen",
  "apartment addition",
  "new kitchen cabinets",
];
---

<section id="gallery">
  <div class="cs-container">
    <span class="cs-topper">{content.eyebrow ?? "Our Portfolio"}</span>
    <h2 class="cs-title">
      {content.title ?? "Et orci volutpat, back up generator installations"}
    </h2>
    <div class="cs-image-group">
      <!-- Row 1-->
      <div class="cs-row cs-row-1">
        <!-- To add more images, copy and paste this row's picture tags here in order from cs-picture-1 to cs-picture-3 and they will maintain the same layout-->
        <Picture
          src={port1}
          alt={imageAlts[0] ?? "new hallway"}
          width={400}
          height={560}
          formats={['avif', 'webp']}
          pictureAttributes={{ class: "cs-picture cs-picture1" }}
        />
        <Picture
          src={port4}
          alt={imageAlts[1] ?? "new home construction"}
          width={400}
          height={560}
          formats={['avif', 'webp']}
          pictureAttributes={{ class: "cs-picture cs-picture2" }}
        />
        <Picture
          src={port7}
          alt={imageAlts[2] ?? "building a new wall"}
          width={400}
          height={560}
          formats={['avif', 'webp']}
          pictureAttributes={{ class: "cs-picture cs-picture3" }}
        />
      </div>
      <!-- Row 2 -->
      <div class="cs-row cs-row-2">
        <!-- To add more images, copy and paste this row's picture tags here in order from cs-picture-1 to cs-picture-3 and they will maintain the same layout-->
        <Picture
          src={port2}
          alt={imageAlts[3] ?? "new kitchen"}
          width={400}
          height={560}
          formats={['avif', 'webp']}
          pictureAttributes={{ class: "cs-picture cs-picture1" }}
        />
        <Picture
          src={port5}
          alt={imageAlts[4] ?? "apartment addition"}
          width={400}
          height={560}
          formats={['avif', 'webp']}
          pictureAttributes={{ class: "cs-picture cs-picture2" }}
        />
        <Picture
          src={port8}
          alt={imageAlts[5] ?? "new kitchen cabinets"}
          width={400}
          height={560}
          formats={['avif', 'webp']}
          pictureAttributes={{ class: "cs-picture cs-picture3" }}
        />
      </div>
      <!-- Row 3 -->
      <div class="cs-row cs-row-3">
        <!-- To add more images, copy and paste this row's picture tags here in order from cs-picture-1 to cs-picture-3 and they will maintain the same layout-->
        <Picture
          src={port3}
          alt={imageAlts[6] ?? "new kitchen"}
          width={400}
          height={560}
          formats={['avif', 'webp']}
          pictureAttributes={{ class: "cs-picture cs-picture1" }}
        />
        <Picture
          src={port6}
          alt={imageAlts[7] ?? "apartment addition"}
          width={400}
          height={560}
          formats={['avif', 'webp']}
          pictureAttributes={{ class: "cs-picture cs-picture2" }}
        />
        <Picture
          src={port9}
          alt={imageAlts[8] ?? "new kitchen cabinets"}
          width={400}
          height={560}
          formats={['avif', 'webp']}
          pictureAttributes={{ class: "cs-picture cs-picture3" }}
        />
      </div>
    </div>
    <a href={content.buttonHref ?? "/projects/"} class="cs-button-solid">{content.buttonText ?? "View all projects"}</a>
  </div>
</section>

<style lang="less">
  /*-- -------------------------- -->
<---          Gallery           -->
<--- -------------------------- -*/

  /* Mobile - 360px - Contains hover animation */
  @media only screen and (min-width: 0em) {
    #gallery {
      /* Centers button */
      text-align: center;
      padding: var(--sectionPadding);
      position: relative;
      /* Prevents overflow from the image going off screen */
      overflow: hidden;
      .cs-container {
        width: 100%;
        max-width: (1322/16em);
        margin: auto;
      }

      .cs-topper {
        text-align: center;
        margin-bottom: (16/16rem);
      }

      .cs-title {
        text-align: center;
        max-width: (621/16rem);
        margin-bottom: (40/16rem);
      }

      .cs-image-group {
        /* used rem so it doesn't scale with the font size of on parent */
        font-size: ~"min(1.1vw, 1em)";
        width: 100%;
        max-width: (1322/16em);
        margin: 0 auto (60/16rem);
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: flex-start;
        gap: (30/16em);
      }

      .cs-row {
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
        align-items: center;
        gap: (30/16em);
      }

      .cs-picture {
        display: block;
        position: relative;
        img {
          width: 100%;
          height: 100%;
          object-fit: cover;
          position: absolute;
          top: 0;
          left: 0;
        }
      }

      .cs-row-1 {
        .cs-picture1 {
          width: (420/16em);
          height: (567/16em);
        }

        .cs-picture2 {
          width: (420/16em);
          height: (629/16em);
        }

        .cs-picture3 {
          width: (420/16em);
          height: (512/16em);
        }
      }

      .cs-row-2 {
        .cs-picture1 {
          width: (420/16em);
          height: (492/16em);
        }

        .cs-picture2 {
          width: (420/16em);
          height: (517/16em);
        }

        .cs-picture3 {
          width: (420/16em);
          height: (629/16em);
        }
      }

      .cs-row-3 {
        .cs-picture1 {
          width: (420/16em);
          height: (625/16em);
        }

        .cs-picture2 {
          width: (420/16em);
          height: (452/16em);
        }

        .cs-picture3 {
          width: (420/16em);
          height: (629/16em);
        }
      }
    }
  }

  /* Dark mode */
  @media only screen and (min-width: 0em) {
    body.dark-mode {
      #gallery {
        .cs-title {
          color: var(--bodyTextColorWhite);
        }
      }
    }
  }
</style>
